import { View } from "react-native";
import { Icon } from "react-native-elements";

export default function Icone(){
    return (
    <View>
        <Icon
        name='user'
        type='evilicon'
        size={200} 
        />
    </View>
    );
}